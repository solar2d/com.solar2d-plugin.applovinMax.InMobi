Sources for the plugin `plugin.applovinMax.InMobi`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.InMobi" = {
            publisherId = "com.solar2d",
        },
    },
}
```
